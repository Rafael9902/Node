const name = "Deadpool";
const real = "Wade Wilson";

const normal = name + " " + real;
const template = `${name} ${real}`

console.log(template);

//permiten trabajar con desarrollo multilinea

const html = `
<h1>Hola</h1>
`